# Instructions

Before doing project-specific work:

1. Run in an interactive shell terminal run `agentnotes` and use the directory it points to.
2. In the agentnotes directory, read `MasterPrompt.md` completely.
3. Follow `MasterPrompt.md` exactly.

Do not rely on chat history alone. If context seems incomplete, stale, or compacted, reload `MasterPrompt.md` and the repository `AgentNotes.md`.
