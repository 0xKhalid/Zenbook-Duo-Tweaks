# The Panther Lake ISH loads before the real root is mounted, so the verified
# ASUS firmware override must be present in the initramfs.
install_items+=" /usr/lib/firmware/updates/intel/ish/ish_ptl.bin "
